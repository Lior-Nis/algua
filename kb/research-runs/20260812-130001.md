# Research run 20260812-130001

Thesis explored: short-horizon mean reversion in liquid names after sharp multi-day adjusted-price
selloffs relative to peers, strongest away from earnings-style volatility spikes.

Evaluation setup:
- Bars snapshot: `ac862071ef99051e`
- PIT universe: `liquid10` via universe snapshot `2978467572a0b38f`
- Window: `2015-01-01` to `2024-01-01`
- Walk-forward: 4 windows, 20% holdout
- Relaxations: none

## calm_four_day_rebound

Hypothesis: buy the sharpest four-day adjusted-price losers only when the prior 20-day realized
volatility is below the cross-sectional median.

Backtest/register: `total_return=0.0000`, `sharpe=0.0000`, `max_drawdown=0.0000`,
`n_rebalances=0`.

Walk-forward summary: `mean_sharpe=0.0000`, `min_sharpe=0.0000`,
`pct_positive_windows=0.0000`.

Sweep: 18 measured combos over `selloff_lookback`, `pre_vol_lookback`, and `construction.top_k`.
Best score was `0.0000`; recorded breadth cumulative for the strategy was 18.

Preview gate: failed. PIT and minimum holdout observations passed, but the binding
`holdout_sharpe_floor` failed with holdout `sharpe=0.0000` and `total_return=0.0000`.

Verdict: discarded. The signal was effectively inert on this PIT universe.

## peer_median_oversold_rebound

Hypothesis: rank names by three-day adjusted-price underperformance versus the liquid10 peer median,
while excluding same-window moves that are spike outliers versus trailing 30-day absolute returns.

Backtest/register: `total_return=0.2930`, `sharpe=0.2461`, `max_drawdown=-0.3383`,
`turnover=1025.5000`, `n_rebalances=1964`.

Walk-forward summary: `mean_sharpe=0.4192`, `min_sharpe=-0.2570`,
`pct_positive_windows=0.7500`.

Sweep: 24 measured combos over `return_lookback`, `vol_lookback`, `spike_z`, and
`construction.top_k`. Best score was `0.5321` at `return_lookback=4`, `vol_lookback=30`,
`spike_z=2.0`, `top_k=3`.

Preview gate: failed. PIT and minimum holdout observations passed, but the binding
`holdout_sharpe_floor` failed with holdout `sharpe=-0.5586` and `total_return=-0.2127`.

Verdict: discarded. The pre-holdout stability did not survive the holdout, and turnover was high.

## orderly_six_day_rebound

Hypothesis: prefer names with deeper six-day adjusted-price losses and at least four down days, but
penalize recent volatility that is elevated versus a trailing 20-day baseline.

Backtest/register: `total_return=-0.3564`, `sharpe=-0.1594`, `max_drawdown=-0.4817`,
`turnover=755.1667`, `n_rebalances=1503`.

Walk-forward summary: `mean_sharpe=-0.1830`, `min_sharpe=-0.7374`,
`pct_positive_windows=0.0000`.

Sweep: 24 measured combos over `selloff_lookback`, `volatility_lookback`, `min_down_days`, and
`construction.top_k`. Best score was `0.8042` at `selloff_lookback=8`,
`volatility_lookback=20`, `min_down_days=4`, `top_k=3`.

Preview gate: passed and promoted to candidate in the scratch registry because all binding checks
passed: PIT universe was present, holdout observations were 452, and raw holdout Sharpe was positive
at `0.0250`. Advisory quality checks were weak: breadth-deflated holdout Sharpe threshold was
`2.6802`, holdout total return was `-0.0320`, `pct_positive_windows=0.0000`,
`min_window_sharpe=-0.7374`, DSR/bootstrap evidence failed, and idiosyncratic alpha was negative.

Verdict: candidate-preview-pass, but low confidence. The gate pass is real under the scratch
integrity floor; the evidence should be treated as a weak candidate needing paper-stage scrutiny,
not as strong confirmation of the thesis.

```json
{"hypotheses":[{"title":"Calm 4-day selloff rebound","verdict":"discarded","merge_back":null},{"title":"Peer-median 3-day oversold rebound","verdict":"discarded","merge_back":null},{"title":"Orderly 6-day selloff rebound","verdict":"candidate-preview-pass","merge_back":{"strategy":"orderly_six_day_rebound","universe":"liquid10","start":"2015-01-01","end":"2024-01-01"}}],"preview_gate":{"passed":true,"failed_checks":[]}}
```
