# Research Run 20260814-050000

Thesis: among liquid equities with similar skip-month momentum, adjusted-price paths that look
like persistent quality compounding should outperform paths driven by noise or a few isolated
jumps. This run used price-only quality proxies because the available bar contract has no clean
adjusted-volume field; raw volume was not used.

Common evaluation context: PIT universe `liquid10`, bars snapshot `b1fb94b29fcb7601`,
`2015-01-01` through `2026-08-14`, `windows=4`, `holdout_frac=0.2`, default fees and slippage,
and `rank_by=mean_sharpe`. Both signals read only sorted `adj_close`, declare a 273-bar feature
lookback, skip the latest 21 sessions, and use `top_k_equal_weight` with `top_k=3`. No relaxation
flags were used.

## 1. Trend fit quality momentum

Strategy: `trend_fit_quality_momentum`

Hypothesis: among names with similar 126-session momentum ending 21 sessions ago, those whose
preceding log adjusted-price path has a stronger linear time-trend fit should outperform names
whose momentum came through a noisier path.

Base parameters: `trend_lookback=252`, `momentum_lookback=126`, `skip_bars=21`, and
`quality_weight=1.0`.

Full PIT backtest:

- Total return: `236.27%`; CAGR: `11.03%`.
- Sharpe: `0.6225`; annual volatility: `20.05%`; max drawdown: `-27.63%`.
- Turnover: `138.83`; rebalances: `410`.

Walk-forward evidence:

- Mean Sharpe: `0.4639`; standard deviation: `0.4549`.
- Minimum window Sharpe: `-0.1229`; positive windows: `75%`.

Measured sweep:

- Exact grid: `trend_lookback=[168,252]`, `quality_weight=[0.5,1.0]` (`4` combos).
- Best: `trend_lookback=168`, `quality_weight=1.0`, mean Sharpe `0.7976`.
- Four-combo mean Sharpe: `0.6431`; annualized trial-Sharpe variance: `0.0289`.
- Recorded strategy breadth after the one sweep: `4`.

Preview gate: not run. The interpretation subagent recommended discarding before spending the
single-use holdout: one of four OOS windows was negative and the authored base configuration was
materially weaker than the shorter-lookback sweep winner. The full-period result did not overcome
that instability.

Verdict: discarded. The useful follow-up would be a genuinely different quality mechanism, not a
post-hoc switch to the winning 168-bar fit.

## 2. Distributed gains quality momentum

Strategy: `distributed_gains_quality_momentum`

Hypothesis: among names with similar 126-session momentum ending 21 sessions ago, momentum whose
positive log-return contribution is distributed across many days should outperform momentum
concentrated in a few jumps. Quality is a bounded normalized inverse-HHI of positive daily return
contributions over the pre-skip adjusted-price window.

Base parameters: `distribution_lookback=252`, `momentum_lookback=126`, `skip_bars=21`, and
`quality_weight=1.0`.

Full PIT backtest:

- Total return: `387.52%`; CAGR: `14.65%`.
- Sharpe: `0.8335`; annual volatility: `18.45%`; max drawdown: `-28.30%`.
- Turnover: `273.50`; rebalances: `791`.

Walk-forward evidence:

- Mean Sharpe: `0.8025`; standard deviation: `0.3223`.
- Minimum window Sharpe: `0.4884`; positive windows: `100%`.

Measured sweep:

- Exact grid: `distribution_lookback=[126,252]`, `quality_weight=[0.5,1.0]` (`4` combos).
- Best: the authored base, `distribution_lookback=252`, `quality_weight=1.0`, mean Sharpe
  `0.8025`.
- Four-combo mean Sharpe: `0.5691`; annualized trial-Sharpe variance: `0.0252`.
- Recorded strategy breadth after the one sweep: `4`.

Scratch preview gate:

- Passed and promoted to `candidate` in the scratch registry.
- PIT check passed; holdout observations `584`; raw holdout Sharpe `0.6438 > 0`.
- Holdout window: `2024-04-16` through `2026-08-13`; return `26.27%`; CAGR `10.59%`;
  annual volatility `18.19%`; max drawdown `-21.98%`.
- Positive-window, minimum-window, holdout-return, and idiosyncratic-alpha advisories passed.
- The gate charged `27` measured funnel/family trials. Advisory failures were the
  breadth-deflated Sharpe (`0.6438 < 2.1865`), DSR evidence (`0.6891 < 0.95`), and DSR bootstrap
  (`0.1603 < 0.95`).

Verdict: candidate-preview-pass. The substantial PIT holdout stayed positive and broadly consistent
with walk-forward evidence, while the base parameters were the measured sweep winner. Confidence is
provisional because statistical breadth advisories failed and turnover is high; the authoritative
replay and later forward gate should remain decisive.

## Summary

Two non-duplicate quality mechanisms were evaluated. Trend fit was discarded without peeking at its
holdout. Distributed-gains quality passed the scratch binding floor and is the only hypothesis named
for authoritative merge-back.

```json
{"hypotheses":[{"title":"Trend fit quality momentum","verdict":"discarded"},{"title":"Distributed gains quality momentum","verdict":"candidate-preview-pass","merge_back":{"strategy":"distributed_gains_quality_momentum","universe":"liquid10","start":"2015-01-01","end":"2026-08-14","eval_context":{"snapshot":"b1fb94b29fcb7601","sweep_grid":{"distribution_lookback":[126,252],"quality_weight":[0.5,1.0]},"rank_by":"mean_sharpe","windows":4,"holdout_frac":0.2}}}],"preview_gate":{"passed":true,"failed_checks":[]}}
```
