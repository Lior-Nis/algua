# Decision log

## 2026-09-25 — Initial distillation

- Chose a transport-independent logical binding so Story 1.3a does not pre-empt Story 1.3c's wire
  encoding. Exact Parquet/JSON byte hashes remain separate operational evidence.
- Bound raw bars and resolved configuration through named full SHA-256 component digests to keep the
  root preimage bounded while committing to every behavior-affecting value.
- Kept `None` versus enabled-empty venue belief as a tagged union because current reconciliation
  semantics distinguish them.
- Kept the strategy implementation as verified pure execution context rather than serializing a
  callable in the request. Deployment/config identities bind the selected implementation.
- Made expected risk/input failures typed boundary values while preserving the supervisor's current
  exception and CLI behavior through an adapter.

Self-validation passed. The kernel satisfies all eight Spec Law rules; all capabilities have
testable success, constraints exclude authority and future-slice leakage, and non-goals are
explicit. Preservation review mapped every load-bearing readiness finding and approved Story 1.3a
decision into the kernel or `planner-contract.md`. No assumption or open question remains. Wrapper-
only workflow ceremony was intentionally omitted.

The source Story 1.3a was updated in the same change to adopt this SPEC and companion as normative,
add the top-level contract table, sharpen AC1 and AC3–AC5, and route its implementation tasks and
references through the closed schema/binding definition.

The readiness quality pass clarified two preservation details: the supervisor retains its static
timeframe fail-fast check before any acquisition while Phase A revalidates the captured value, and
the bar digest commits to exact captured row order before Phase A performs the baseline stable index
sort. This removes a pre-effect parity contradiction without moving behavior or authority.

The same pass found the current freshness wall's hidden settings read. `calendar_code` is now an
explicit, bound early input; the planner constructs the config-free calendar leaf and cannot read
operational settings. This closes the last known behavior-affecting implicit input without changing
the configured calendar choice.
