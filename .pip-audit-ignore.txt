# pip-audit advisory allowlist  (consumed by .github/workflows/security.yml)
#
# One vulnerability ID per non-comment line. Each entry is a KNOWN advisory that
# cannot currently be remediated by a lockfile bump, with a documented reason.
# Keep this list MINIMAL and review the entries at every dependency upgrade.
#
# NOTE ON SCOPE: pip-audit's --ignore-vuln suppresses an advisory GLOBALLY (it is
# not package-scoped). Because the list is small and reviewed, that is acceptable;
# do not add an ID here without confirming which package/version it applies to.
#
# Format for each active entry (all lines above the ID are comments):
#   # <package> <version> — <one-line reason> — tracking: <issue/url> — review: <YYYY-MM>

# ---------------------------------------------------------------------------
# starlette 0.52.1 — vulnerable, but the fix ships only in starlette 1.x (a MAJOR
# release). starlette is pinned transitively via mlflow -> fastapi and cannot be
# upgraded without a major mlflow-tree bump. Clears when mlflow is next
# major-upgraded. tracking: #395 — review: 2026-10
PYSEC-2026-161
PYSEC-2026-248
PYSEC-2026-249
GHSA-wqp7-x3pw-xc5r
GHSA-x746-7m8f-x49c

# ---------------------------------------------------------------------------
# cryptography 46.0.7 — pinned transitively via mlflow -> databricks-sdk -> google-auth;
# does not resolve to the fix lines (48.0.1 / 49.0.0 / 50.0.0) without a major mlflow-tree
# bump. Clears when mlflow is next major-upgraded. tracking: #395 — review: 2026-10
#
# The four advisories below are all in cryptography's x509/pkcs7 verification of
# UNTRUSTED input (S/MIME EnvelopedData decryption; attacker-controlled certificate-chain
# validation). algua never does this — cryptography is a transitive TLS/JWT dependency to
# FIXED hosts (Alpaca + our own data), not a verifier of attacker-supplied certs/messages —
# so none is reachable in algua's usage. Ignored pending the mlflow-tree major bump.
GHSA-537c-gmf6-5ccf
# PYSEC-2026-3552 — pkcs7_decrypt Bleichenbacher oracle (S/MIME decrypt of untrusted input; fix 50.0.0)
PYSEC-2026-3552
# PYSEC-2026-3553 — cert-chain validation DoS on duplicate self-signed certs (fix 49.0.0)
PYSEC-2026-3553
# PYSEC-2026-3554 — name-constrained wildcard SAN escape in cert verification (fix 49.0.0)
PYSEC-2026-3554

# ---------------------------------------------------------------------------
# mlflow 3.12.0 — GHSA-2cm6-r77w-6g96: trace API endpoints skip authorization
# validators WHEN RUNNING `mlflow server` WITH AUTH ENABLED. algua never runs an
# mlflow server: tracking is the local FileStore (`mlruns/`, direct writes via
# MlflowTracker), no server process, no auth layer, no network surface — the
# vulnerable handler is unreachable in this deployment. The only fix release is
# 3.13.0rc0 (an RC), and bumping mlflow churns the root uv.lock =
# dependency_hash identity (invalidates gate/live-auth artifact stamps), so the
# upgrade waits for the next planned mlflow-tree bump. tracking: #395 — review: 2026-10
GHSA-2cm6-r77w-6g96
# PYSEC-2026-3686 — the PYSEC ALIAS of the GHSA directly above: same trace-authz hole, same
# 3.13.0rc0 fix line, same unreachability. Listed separately because pip-audit suppresses by
# the ID STRING, not by the underlying advisory — so an advisory already reviewed and
# allowlisted under one ID silently re-appears (and re-reds CI) the day OSV publishes its
# alias. It did: this entry was added 2026-08-24, after a CI run the previous day reported
# only the GHSA. When reviewing this list, check aliases, not just IDs.
PYSEC-2026-3686

# ---------------------------------------------------------------------------
# mlflow 3.12.0 — UNREACHABLE. PYSEC-2026-3687 is an unauthenticated full-read SSRF in the
# mlflow TRACKING SERVER's model-registry webhooks API (`mlflow server`, default no-auth): the
# `_validate_webhook_url` guard resolves and rejects non-public IPs but pins nothing, so a
# public endpoint returning `302 Location: http://169.254.169.254/...` is followed and reflected.
# Algua NEVER starts an mlflow server and never registers a webhook: the only tracking surface is
# the FileStore adapter behind the `ExperimentTracker` seam (`algua/tracking/mlflow_tracker.py`),
# driven by `settings.mlflow_tracking_uri = "mlruns"` — a local directory, no HTTP listener, no
# webhook registry. Same reasoning and same reachability boundary as the already-allowlisted
# GHSA-2cm6-r77w-6g96 (mlflow server trace-authz hole, #561).
#
# NOT fixed by a lockfile bump for a NON-obvious reason worth recording: the fix line (3.15.0+)
# hard-deprecates the filesystem backend — mlflow 3.15.1 raises
# "The filesystem tracking backend (e.g., './mlruns') is in maintenance mode" unless
# MLFLOW_ALLOW_FILE_STORE=true. Measured: bumping to 3.15.1 turns 52 tracking tests red. Taking
# the fix therefore means either opting out of the deprecation or migrating the store to a sqlite
# backend — a design decision about the component-tracking layer, not a dependency bump.
# tracking: #395 — review: 2026-10
PYSEC-2026-3687
